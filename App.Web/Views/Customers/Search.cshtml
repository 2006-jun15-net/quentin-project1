@model List<App.Web.Models.ViewModels.CustomerVM>
@{
    ViewData["Title"] = "Search";
}

<h1>Search</h1>
<table class="table" summary="Customer Search Results">
    <thead>
        <tr>
            <th scope="col">
                Name
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model[0].LocationName)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.FirstName @item.LastName
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LocationName)
                </td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<h2><a href="/Home/Search">Back To Search</a></h2>